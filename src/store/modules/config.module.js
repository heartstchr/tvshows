// initial state
const state = {
  ENDPOINT : 'http://api.tvmaze.com/'
}

// getters
const getters = {
  ENDPOINT: state => state.ENDPOINT,
}
// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  state,
  getters,
  actions,
  mutations
}
